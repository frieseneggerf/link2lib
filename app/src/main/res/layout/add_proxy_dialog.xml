<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="20dp">

    <EditText
        android:id="@+id/editTextProxyDesc"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:autofillHints="@string/proxy_description"
        android:hint="@string/proxy_description"
        android:inputType="text" />

    <EditText
        android:id="@+id/editTextProxyUrl"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:autofillHints="@string/proxy_url"
        android:hint="@string/proxy_url"
        android:inputType="textUri"
        android:layout_marginTop="16dp"/>
</LinearLayout>